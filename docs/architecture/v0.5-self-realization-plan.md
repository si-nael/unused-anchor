# v0.5 Self-Realization Release

Date: 2026-07-15

Status: completed and verified on 2026-07-15

## Corrected Priority

The primary objective is to complete the bubble universe itself. A scored observer or host-language comparison is Phase 2 and must not displace unfinished world semantics.

`v0.5` therefore opens the first large vertical world flow:

`authored state -> lawful possibilities -> world-will evaluation -> realization status -> continuation -> causal evidence -> optional history commit -> inspection/replay`

No external scheduler, observer, agent, or hidden random selector owns this transition.

## Completion Gate

The baseline is complete when:

- `state` and reversible/irreversible `transform` compile into versioned IR
- executable world will evaluates projected candidates
- deterministic ambiguity stays underdetermined
- nondeterministic plurality stays plural
- reciprocal reversibility is compiler-checked
- universal time is not assumed
- only a selected commit realization creates the history arrow and runtime commit
- self-realization is visible in proof, evidence, attribution, inspection, replay, and canonical artifacts
- dates, ideas, decisions, questions, hypotheses, work, and boundary failures are reconciled without deleting older branches
- mandatory repository verification passes as `unused-anchor@0.5.0`

## `v0.5.1` Continuation Queue

Closure of `v0.5` does not close these ideas:

1. execute branch, spawn, and collapse continuations as concrete world transitions rather than only typed continuation topology
2. support coupled transformations across multiple state coordinates
3. add bounded autonomous continuation traversal without installing a universal clock or external chooser
4. derive partial causal order, cycles, and stationarity as explicit inspectable structures
5. feed selected realization consequences back into sea and anchor dynamics more deeply
6. strengthen same-world resume and deterministic re-execution proof

Each `v0.5.x` release should close one of these seams with code, evidence, tests, artifacts, and reconciled records. Unselected ideas remain live.

### Completed v0.5.1 direction

The queue above remains historical and live, but the author's later clarification prevents selecting one item as an isolated feature. `v0.5.1` completed a bounded connected anchored narrative-world seam in which protagonists, story, World Will, anchors, seas, and causal order constrain one another. The architecture, executable evidence, limits, and reopen frontier are recorded in [v0.5.1 Anchored Narrative World Architecture](v0.5.1-anchored-narrative-world.md).

The exact intensional-family substrate now feeds formal fields, state-bound constraints, anchor identity, and deterministic replay. This bounded connection does not complete persistent narrative, global sea dynamics, cross-world anchors, or the whole queue.

### Completed v0.5.2 correction

The author clarified that the protagonist/story formulation was explanatory, not the universal minimum ontology. `v0.5.2` therefore preserves the completed narrative seam as a special case and adds an actor-neutral causal kernel in which state-bound laws generate events and generic structures can emerge without being named in advance. See [v0.5.2 Generative Causal Universe](v0.5.2-generative-causal-universe.md). Persistent beings, agency, relationships, and narrative remain required possibilities whose derivation is still open; they were not removed.

### Completed v0.5.3 persistence seam

`v0.5.3` preserves the actor-neutral kernel and adds an exact closure-coalgebra layer. It derives recurrent field projections from conjunctive law dependencies, requires boundary-mediated causal hypercuts and effective cut ablation, unfolds every lawful continuation, separates causal-configuration recurrence from monotone anchor history, and requires invariant identity, universally effective memory erasure, recurrent-reference restoration after negative-sea deviation, and outward-influence evidence before calling a component persistent. See [v0.5.3 Persistent Causal Structure](v0.5.3-persistent-causal-structure.md). Goals, affordance choice, agency, relations, narrative projection, multi-world seas/anchors, and whole-universe generation remain open.

## Completion Evidence

- package and lockfile are `0.5.0`
- canonical reversible and irreversible worlds materialize and inspect as intended
- the commit example survives stored record/replay with the same self-realization frontier
- the full `npm.cmd run verify` path passed on 2026-07-15
- all 100 unit and research-record tests passed
- every mandatory compile, materialize, inspect, record, and replay command completed
